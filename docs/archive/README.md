# Documentation Archive

This folder contains historical planning notes, superseded implementation records, and stale sprint-specific runbooks that are no longer part of the main operator documentation surface.

Canonical current docs remain in the repo root and `docs/`.

- Use `README.md` for the top-level product and setup overview.
- Use `docs/deployment-guide.md` for deployment/runtime guidance.
- Use `docs/local-reset-workflow.md` for disposable local backend resets.
- Use `docs/release-gates.md` and `docs/security-preproduction-checklist.md` for pre-release verification.
